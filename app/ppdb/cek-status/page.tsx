"use client";

import { FormEvent, useState } from "react";
import { Panel, SubpageShell } from "@/app/components/subpage-shell";

export default function StatusPage() { const [result, setResult] = useState<string | null>(null); const check = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const number = new FormData(event.currentTarget).get("number")?.toString().trim().toUpperCase() ?? ""; const saved = localStorage.getItem(`ppdb:${number}`); setResult(saved ? "Menunggu verifikasi" : "Nomor belum ditemukan. Pastikan penulisan sudah benar."); }; return <SubpageShell eyebrow="Pelacakan Pendaftaran" title="Cek Status Berkas" intro="Masukkan nomor pendaftaran yang kamu dapat setelah mengirim formulir."><section className="mx-auto max-w-xl px-5 py-14 sm:px-8"><Panel><form onSubmit={check}><label className="block text-sm font-bold">Nomor pendaftaran<input required name="number" placeholder="Contoh: BM26-123456" className="mt-2 block w-full border-[3px] border-ink px-4 py-3 uppercase outline-none" /></label><button className="mt-6 w-full border-[3px] border-ink bg-ink px-5 py-3 font-bold text-paper comic-shadow">Lihat Status</button></form>{result && <div className="mt-7 border-[3px] border-ink bg-paper-soft p-5"><p className="text-sm font-bold">Status pendaftaran</p><p className="mt-2 font-display text-3xl">{result}</p></div>}</Panel></section></SubpageShell>; }
