"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { defaultSchedule } from "@/app/lib/ppdb-data";
import { Panel, SubpageShell } from "@/app/components/subpage-shell";

export default function PpdbPage() { const [schedule, setSchedule] = useState(defaultSchedule); useEffect(() => { const saved = localStorage.getItem("ppdb:schedule"); if (saved) setSchedule(JSON.parse(saved)); }, []); return <SubpageShell eyebrow="Penerimaan Peserta Didik Baru" title="Mulai Langkahmu di Sini" intro="Pendaftaran tahun ajaran baru dibuka untuk calon siswa yang siap belajar, praktik, dan berkembang bersama."><section className="mx-auto max-w-5xl px-5 py-14 sm:px-8"><div className="grid gap-6 md:grid-cols-2"><Panel><h2 className="font-display text-4xl text-primary">JADWAL TERBARU</h2><div className="mt-5 space-y-3 text-sm">{schedule.map(([stage, first, second]) => <div key={stage} className="border-b-2 border-primary/20 pb-3"><p className="font-bold">{stage}</p><p className="mt-1 text-neutral-700">Gelombang 1: {first}<br />Gelombang 2: {second}</p></div>)}</div></Panel><Panel><h2 className="font-display text-4xl">ALUR</h2><p className="mt-4 leading-7">Isi formulir, upload dokumen, verifikasi berkas, lalu pantau pengumuman secara online.</p><Link href="/ppdb/daftar" className="mt-6 inline-flex border-[3px] border-primary bg-primary px-5 py-3 font-bold text-paper comic-shadow">Buka Formulir</Link></Panel><Panel><h2 className="font-display text-4xl">LAYANAN PPDB</h2><div className="mt-4 flex flex-col gap-3 text-sm"><Link href="/ppdb/pembayaran" className="font-bold text-primary underline underline-offset-4">Konfirmasi pembayaran</Link><Link href="/ppdb/ranking" className="font-bold text-primary underline underline-offset-4">Lihat ranking seleksi</Link><Link href="/ppdb/pengumuman" className="font-bold text-primary underline underline-offset-4">Pengumuman kelulusan</Link><Link href="/faq" className="font-bold text-primary underline underline-offset-4">Baca FAQ pendaftaran</Link></div></Panel><Panel><h2 className="font-display text-4xl">SUDAH DAFTAR?</h2><p className="mt-4 leading-7">Gunakan nomor pendaftaran untuk melihat status berkas.</p><Link href="/ppdb/cek-status" className="mt-6 inline-flex border-[3px] border-primary px-5 py-3 font-bold text-primary comic-shadow">Cek Status</Link></Panel></div></section></SubpageShell>; }
